## Database configuration is intentionally omitted here.
## Use the `hk-db-dev` docker compose for the development database.

# ── JWT Authentication ──
JWT_SECRET=your_super_secret_jwt_string_here
JWT_EXPIRY=7d

# ── Domain & Environment ──
DOMAIN=localhost
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1
