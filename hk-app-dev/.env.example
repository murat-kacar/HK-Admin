## Database configuration is intentionally omitted here.
## Use the `hk-db-dev` docker compose for the development database.

# ── JWT Authentication ──
JWT_SECRET=your_super_secret_jwt_string_here
JWT_EXPIRY=7d

# ── Domain & Environment ──
DOMAIN=localhost
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1

# ── Storage Configuration ──
# Options: 'local' or 'r2'
STORAGE_TYPE=local

# ── Cloudflare R2 (only needed if STORAGE_TYPE=r2) ──
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=hk-media-dev
R2_PUBLIC_URL=https://media.hkakademi.com

# ── Media Processing ──
# Set to true for AVIF format (better compression but slower processing)
ENABLE_AVIF=false
