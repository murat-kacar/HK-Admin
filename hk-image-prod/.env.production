
# ── External Database ──
# Configure external DB connection in deployment environment variables
# Do NOT commit credentials here.

# ── App Secrets ──
JWT_SECRET=production_super_secret_key_change_me
JWT_EXPIRY=7d
DOMAIN=your-domain.com

# ── Environment ──
NODE_ENV=production
PORT=3000

# ── External Storage (Optional for future) ──
# STORAGE_PROVIDER=s3
# S3_BUCKET=...
